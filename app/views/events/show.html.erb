<% if current_user == @event.host.owner %>
  <%= render partial: 'events/host_view', locals: { event: @event } %>
<% else %>
  <%= render partial: 'events/std_view', locals: { event: @event } %>
<% end %>
