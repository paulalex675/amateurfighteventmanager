<h3>Comments<h3>

<% commentable.comments.each do |comment| %>
  <p><%= comment.body %><span>
 <strong> <%= link_to "#{comment.auther.first_name}", user_path(comment.auther_id) %>  </strong></p>
<% end %>